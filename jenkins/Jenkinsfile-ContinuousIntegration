pipeline {
    agent { docker { image 'maven' } }
    stages {
        stage('Build & Test') {
            steps {
                sh 'mvn test'
            }
        }
    }
    post {
        always {
            junit 'target/surefire-reports/*.xml'
        }
    }
}
